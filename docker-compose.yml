services:
  dolar_api:
    build: .
    tty: true
    stdin_open: true
    restart: unless-stopped
    container_name: dolar_api
    ports:
      - "${APPPORT}:3000/tcp"
    environment:
      APPPORT: "3000"
      APPENDPOINT: "${APPENDPOINT}"
      BROWSERPATH: "/usr/bin/chromium"